class Ability:
    def __init__(self, Algorithm, affinity, so_path, ir_type):
        self.Algorithm = Algorithm
        self.affinity = affinity
        self.so_path = so_path
        self.ir_type = ir_type